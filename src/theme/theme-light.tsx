/* eslint-disable object-shorthand */
/* eslint-disable @typescript-eslint/indent */
import {
    headerH1,
    navItemsColor,
    inputValuesColor,
    labelColor,
    mainButtonsColor,
    secondaryButtonsColor,
    bgColor,
    actionButtonsColor,
    sidebarColor,
    inputBg,
} from '../services/constants/colors';

import { TTheme } from '../types/theme-types';

const themeLight: TTheme = {
    headerH1: headerH1,
    navItemsColor: navItemsColor,
    inputValuesColor: inputValuesColor,
    labelColor: labelColor,
    mainButtonsColor: mainButtonsColor,
    secondaryButtonsColor: secondaryButtonsColor,
    bgColor: bgColor,
    actionButtonsColor: actionButtonsColor,
    sidebarColor: sidebarColor,
    inputBg: inputBg,
};

export default themeLight;
